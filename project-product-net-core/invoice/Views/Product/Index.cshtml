@model IEnumerable<invoice.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MOTA_SP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GIA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SOLUONG)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MOTA_SP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GIA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SOLUONG)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_SP">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_SP">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_SP">Delete</a>
                @*<a asp-action="Export" asp-route-id="">Export</a>*@
                <div class="btn">
            @*@{Html.BeginForm("ExportToPDF", "Product", FormMethod.Post);
                {
                    <input type="submit" class="btn" value="Export To PDF"/>
                }
            }*@
        </div>
                @*<form method="post" asp-page="Index" asp-page-handler="ExportToPDF">
                    <div>
                        <input type="submit" value="Create Document" style="width:150px;height:27px" />
                    </div>
                </form>*@
            </td>
        </tr>
}
    </tbody>
</table>
